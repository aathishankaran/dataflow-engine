# PySpark Dataflow Engine
# Python 3.6.8+  |  Linux / macOS / Windows

# ── Core ──────────────────────────────────────────────────────────────────────
# PySpark 3.1.x is the last release supporting Python 3.6.
pyspark>=3.0.0,<3.2

# ── AWS S3 support (optional — needed when reading/writing s3:// paths) ───────
boto3>=1.20.0,<1.27      # boto3 1.27+ requires Python 3.7+
# NOTE: hadoop-aws and aws-java-sdk-bundle JARs (Java) cannot be pip-installed.
# Place the following JARs into PySpark's jars/ directory before running:
#   hadoop-aws-3.3.1.jar             (compatible with PySpark 3.1.3 / Hadoop 3.2.0)
#   aws-java-sdk-bundle-1.11.901.jar
# Find jars dir: python -c "import pyspark,os; print(os.path.join(os.path.dirname(pyspark.__file__),'jars'))"

# ── HashiCorp Vault integration (vault.py) ────────────────────────────────────
hvac>=0.11.0,<1.0        # hvac 1.0+ requires Python 3.7+

# ── HTTP / incident management (incident.py — MoogSoft/ServiceNow) ────────────
requests>=2.25.0,<2.28   # requests 2.28+ requires Python 3.7+

# ── Data utilities ─────────────────────────────────────────────────────────────
pyarrow>=3.0.0,<6.0      # pyarrow 6.0+ requires Python 3.7+

# ── COBOL / EBCDIC mainframe support (optional — Cobrix Spark package) ─────────
# Install the Cobrix JAR via spark-submit --packages instead of pip:
#   --packages za.co.absa:cobrix-spark_2.12:2.7.6
# Python wrapper (optional convenience):
# pip install spark-cobrix

# ── Development / testing extras (not required in production) ──────────────────
pytest>=3.0,<5.0         # pytest 5.0+ requires Python 3.5+ but 4.6 is last for 3.6
# pytest-spark>=0.6.0
